## Scripts

This is a folder of reusable script functions. The overall scripts should not be shared between runs, as they serve as documentation for the benchmarking run. 

Once a function is created it should not be edited as the reusability of the scripts requires that they stay constant. If modifications are necessary, create a new function with a Version number attached. 

