function rank_println(to_print, rank)
    if rank == 0
        println(to_print)
    end
end

function rank_print(to_print, rank)
    if rank == 0
        print(to_print)
    end
end

function rank_display(to_display, rank)
    if rank == 0
        display(to_display)
    end
end